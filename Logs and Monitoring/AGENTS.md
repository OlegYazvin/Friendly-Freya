# Packaged Artifact Instructions

This directory contains non-authoritative build artifacts and an extracted
Windows snapshot.

- Do not implement source changes under `FriendlyFreya_Windows/`.
- Do not treat its Markdown as current project guidance.
- Use the repository-root `README.md`, `AGENTS.md`, and `godot/` source tree.
- Edit or replace packaged artifacts only when the user explicitly requests a
  Windows export/package operation.
